'''
Imports necessary components from the pipeline, app factory, and
models to set up and run the Dash application.
'''

from .pipeline import data_pipeline
from .app import app_factory
from .models import children
